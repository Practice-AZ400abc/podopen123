import React from 'react'

export const metadata = {
    title: "LookVisa – Visa Sponsor Settings ",

};

const layout = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default layout