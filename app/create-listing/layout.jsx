import React from 'react';

export const metadata = {
    title: "Lookvisa – Visa Sponsor Listings",
    description: "LookVisa is the premier platform that brings together immigrant visa visitors with foreign projects requiring investment for an immigrant visa, golden visa, or EB5 visa.",
    keywords: "EB5, Golden visa, citizenship investment, visa sponsor, immigrant investor",
};

const Layout = ({ children }) => {
    return (
        <>
            {children}
        </>
    );
}

export default Layout;